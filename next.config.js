/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          "cdn-icons-png.flaticon.com",
          "image.flaticon.com",
        ],
      },
};

module.exports = nextConfig;
