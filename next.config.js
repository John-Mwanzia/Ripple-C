/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
     domains :[
      "cdn-icons-png.flaticon.com",
      "images.flaticon.com",
      "images.unsplash.com"
     ]
      },
};

module.exports = nextConfig;
